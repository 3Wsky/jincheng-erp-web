# 锦程 ERP 开发约束

1. 库存、资金、责任变化必须由业务单据驱动，禁止直接修改余额作为业务实现。
2. 序列号商品必须一机一码；IMEI/SN 在公司范围内唯一。
3. 跨角色流程必须按状态机和握手确认实现，禁止用单个“已完成”字段替代。
4. 业务写入必须同时落审计日志；重要历史数据使用冲销/作废，不做物理删除。
5. 权限检查必须同时覆盖 Role、DataScope、Action、Field 与 Approval。
6. 外部平台事实数据保留原始 ID、同步批次和原始载荷，不允许员工手工覆盖。
7. 所有对外 API 维护 OpenAPI 契约；未来 PC、APP、小程序复用同一业务 API。
8. 销售单来源与毛利口径未确认前，不冻结最终销售数据库结构。
9. 每个需求必须关联测试用例与验收编号；合并前至少通过类型检查、单元测试和关键流程集成测试。
10. 生产数据库迁移必须可回滚或有前向修复方案，并在测试环境完成演练。
11. 开工前阅读 `docs/README.md`；遇到 `待确认` 或 `BLOCKED` 项不得自行假设为最终业务规则。
12. 修改状态机、金额、权限或接口时，同一提交内更新对应文档、测试用例和需求追踪关系。
